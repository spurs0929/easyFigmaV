# NOTE: keep this file ASCII-only.
# pip decodes requirements files with the system locale encoding (cp950 on
# zh-TW Windows), not UTF-8, so non-ASCII comments break `pip install -r`.

fastapi==0.115.0
uvicorn[standard]==0.32.0

# The [asyncio] extra pulls in greenlet explicitly. SQLAlchemy's async support
# needs it, but in the base package it is a conditional dependency that varies
# by platform and architecture, so it is not guaranteed to be installed.
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# [email] extra provides EmailStr validation.
pydantic[email]>=2.9,<3
pydantic-settings==2.6.0
alembic==1.14.0

# Auth
argon2-cffi==23.1.0
pyjwt==2.10.1