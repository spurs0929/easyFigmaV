# NOTE: keep this file ASCII-only (see requirements.txt for the reason).
-r requirements.txt

pytest==8.3.3
# asyncio_mode = "auto" is configured in pyproject.toml, so async test
# functions do not need an explicit marker.
pytest-asyncio==0.24.0
# ASGITransport lets the test client call the app in-process, with no port
# binding and no live server to wait on.
httpx==0.27.2